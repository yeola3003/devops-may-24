pipeline {
    agent any

    stages {
        stage('login to ecr') {
            steps {
                // Use the AWS CLI to get login credentials
                script {
                    def awsAccountId = "my-aws"
                    def awsRegion = ""
                    def ecrRepository = "http://ecr.com"
        stage (build) {
            steps {
                Step {
                 echo “building project”
            }]
        stage (test) {
            steps {
                Step {
                 echo “testing project”
            }
        stage (deploy) {
            steps {
                Step {
                 echo “deploying project”
            }]    

        }
     }
}
    
